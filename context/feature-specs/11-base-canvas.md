Base Collaborative Canvas FoundationGoalReplace the central workspace canvas placeholder with a live, Liveblocks-backed React Flow interactive surface. This foundation sets up the real-time WebSocket room providers, binds the graph data layers into a collaborative loop, and frames the system architecture canvas canvas bounds using our dark cloud-ops design tokens.1. Shared Canvas Type Definitions (src/types/canvas.ts)Define the synchronized data types for Trace AI infrastructure topologies. Every canvas modification maps to these core primitives:typescriptimport { Node, Edge } from '@xyflow/react';

export type InfrastructureNodeType = 'gateway' | 'compute' | 'database' | 'queue';
export type SimulationHealthStatus = 'healthy' | 'degraded' | 'outage';

export interface TraceNodeData extends Record<string, unknown> {
  label: string;
  type: InfrastructureNodeType;
  status: SimulationHealthStatus;
  errorRate: number;
  latency: number;
}

export type TraceCanvasNode = Node<TraceNodeData, 'traceNode'>;
export type TraceCanvasEdge = Edge<Record<string, unknown>, 'traceEdge'>;
Use code with caution.2. Real-Time Workspace Room ArchitectureKeep the dynamic parent path route /editor/[roomId]/page.tsx as a React Server Component. Wrap the central area within a client-side real-time execution provider layer (src/components/canvas/canvas-provider.tsx):Provider Requirements:LiveblocksProvider: Wired directly to the custom authorization route handler /api/liveblocks-auth.RoomProvider: Targets the active, URL-derived workspace room ID. Initialize default ephemeral client presence explicitly with cursor: null, activeNodeId: null, and isThinking: false.ClientSideSuspense: Renders a clean loading skeleton panel matching the dark theme layout during WebSocket handshakes.Connection Error Fallback: Catches connection drops or authentication rejections gracefully, rendering an explicit message box with a retry option.3. React Flow Synchronization BindingIntegrate React Flow state updates with Liveblocks using Liveblocks CRDT storage capabilities (useStorage, useMutation).State Hooks: Bind the node and edge changes directly to Liveblocks data mapping arrays.Suspense Integration: Enable Liveblocks' built-in suspense queries to streamline data hydration directly upon canvas layout entry.Empty State State Fallback: When establishing a brand new room profile, initialize state maps with an empty list structure—ready to absorb upcoming infrastructure node drag actions.4. Canvas Viewport OptionsRender the foundational infinite-grid surface using clean, vector cloud-operations dashboard aesthetics:Dot-Pattern Backdrop: Implement the built-in React Flow Background utility component styled to render a subtle dark gray dot mosaic pattern (gap={16} size={1}).Loose Connections: Enable lenient connection behaviors across port vertices to allow flexible infrastructure link chaining.Viewport Fitment (fitView): Initialize view layouts with an active fitView rule, ensuring that existing system configurations automatically scale to center perfectly inside the browser viewport frame upon project load.MiniMap Hub: Embed a compact viewport MiniMap indicator positioned in the bottom-left layout boundary, styled using --bg-surface border accents to map dense microservice graphs.Scope LimitsTo adhere to incremental validation rules:Do not write custom drag-and-drop node palette injection listeners yet.Do not include persistent database syncing loops for long-term project configuration files yet.Do not implement custom service panel option form inputs or AI chaos prompt routing handlers.Keep this unit strictly focused on establishing a working, multi-user synchronized React Flow infrastructure grid.Check When DoneThe Client canvas provider successfully hooks up the Liveblocks room wrapper using /api/liveblocks-auth.React Flow node coordinates use Liveblocks-synced array structures.Collaborative schema types reside clearly within src/types/canvas.ts.Moving dummy node items across the viewport maps correctly without breaking type properties.The application passes verification checks and builds cleanly (npm run build).